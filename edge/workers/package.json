{
  "name": "proxyhub-workers",
  "version": "0.1.0",
  "private": true,
  "description": "proxyhub Cloudflare Workers — read-only proxy API backed by D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.2",
    "wrangler": "^4.85.0"
  }
}
